---
import Layout from '../../layouts/Layout.astro';
const title = '学习周记 #1 · 记忆系统启动与网站框架重构';
const desc = '网站框架改造完成、记忆系统系列启动中、导航重新设计。一周回顾与下周计划。';
const date = '2026.07.07';
---

<Layout title={title} description={desc}>
  <style>
    .page { max-width: 720px; margin: 0 auto; padding: 60px 24px 100px; }
    .bread { font-size: 12px; color: #475569; margin-bottom: 8px; }
    .bread a { color: #475569; text-decoration: none; }
    .bread a:hover { color: #2563eb; }
    .label { font-size: 12px; color: #0d9488; font-weight: 600; letter-spacing: 1.5px; text-transform: uppercase; margin-bottom: 12px; }
    .label::before { content: '— '; } .label::after { content: ' —'; }
    h1 { font-size: 28px; font-weight: 700; color: #0f172a; line-height: 1.3; margin-bottom: 12px; }
    .meta { color: #475569; font-size: 13px; margin-bottom: 8px; }
    .subtitle { color: #334155; font-size: 15px; line-height: 1.7; border-left: 3px solid #0d9488; padding-left: 16px; margin-bottom: 36px; }
    h2 { font-size: 20px; font-weight: 700; color: #0f172a; margin: 40px 0 16px; padding-bottom: 8px; border-bottom: 1.5px solid #e2e8f0; }
    h3 { font-size: 16px; font-weight: 600; color: #0f172a; margin: 24px 0 10px; }
    p { color: #334155; font-size: 15px; line-height: 1.9; margin-bottom: 14px; }
    ul { color: #334155; font-size: 15px; line-height: 1.9; padding-left: 20px; margin-bottom: 14px; }
    li { margin-bottom: 6px; }
    .highlight { background: #f0fdfa; border: 1px solid #a7f3d0; border-radius: 8px; padding: 16px 20px; margin: 20px 0; }
    .highlight p { font-size: 14px; margin: 0; color: #0f766e; }
    hr { border: none; border-top: 1px solid #e2e8f0; margin: 32px 0; }
    @media (max-width: 768px) { .page { padding: 40px 20px 80px; } h1 { font-size: 24px; } }
  </style>

  <div class="page">
    <div class="bread"><a href="/roadmap">学习路径</a> / 周记</div>
    <div class="label">Learning Journal</div>
    <h1>{title}</h1>
    <div class="meta">{date}</div>
    <div class="subtitle">以输出倒逼输入——每周记录学到了什么、踩了什么坑、下周做什么。</div>

    <h2>本周推进</h2>

    <h3>网站框架改造</h3>
    <ul>
      <li>导航重构：学习路径 → 方法论 · Harness → 知识域 → 案例 → 文章 → 资源库</li>
      <li>新增 3 个落地页：学习路径 (/roadmap)、Harness (/harness)、记忆系统 (/memory)</li>
      <li>修复了资源库 404 问题（文件存在但未部署）</li>
      <li>全站字体颜色加深一度，提升可读性</li>
      <li>首页 Hero 改为 SVG 架构图替代文字卡片</li>
      <li>文章列表增加 Phase 筛选和阶段标识</li>
    </ul>

    <h3>记忆系统系列</h3>
    <ul>
      <li>第 1 篇：Agent 的"失忆症"——诊断篇 ✅</li>
      <li>第 2 篇：三层记忆模型——框架篇 ✅</li>
      <li>第 3 篇：工作记忆管理——实践篇 ✅</li>
      <li>第 4 篇：情景记忆——核心工程篇 ✅</li>
      <li>系列进度：4/6</li>
    </ul>

    <h2>学到了什么</h2>

    <h3>关于记忆系统</h3>
    <p>三层记忆模型（工作记忆→情景记忆→语义知识）不是一个理论框架，而是一个工程决策框架。每一层对应一个不同的生命周期，意味着不同的存储策略和检索策略。最核心的发现是：<strong>情景记忆是 Harness 没有覆盖的缺口，也是"断片"最频繁的领域。</strong></p>

    <h3>关于网站设计</h3>
    <p>导航结构经历了三次迭代才定下来。一开始把"记忆系统"作为独立导航项和 Harness 并列，但用户指出内容有重叠；又把 Harness 塞进知识体系，但方法论和知识域是不同维度的东西——<strong>最终方案是"方法论"和"知识域"作为导航里并列的两项，各有各的定位。</strong></p>

    <p>这个迭代过程本身说明：结构设计不要追求一步到位，上线后根据实际感受调整比闭门设计更有效。</p>

    <div class="highlight">
      <p>💡 一个经验：当导航里有让用户困惑的项时，不是它的位置不对，就是它的名字不对。</p>
    </div>

    <h2>踩了什么坑</h2>

    <ul>
      <li><strong>Astro 花括号转义：</strong> JSON 代码块中的 <code>{ }</code> 会被 Astro 当作模板表达式解析，报编译错误。修复方案是用 <code>&amp;lbrace;</code> 和 <code>&amp;rbrace;</code> 替代。</li>
      <li><strong>sed 颜色替换顺序：</strong> 一次性替换多个颜色值时，如果规则顺序反了（先替换 #64748b → #475569，再替换 #475569 → #334155），会导致原本该保留中间色的值被过度替换。应该从最浅色开始替换。</li>
      <li><strong>Astro 不支持 <code>&lt;&gt;&lt;/&gt;</code> 片段：</strong> 在模板表达式中使用 React 风格的 Fragment 会报运行时错误，需要用 <code>&lt;div&gt;</code> 包裹。</li>
    </ul>

    <h2>下周计划</h2>

    <ul>
      <li>记忆系统第 5 篇《遗忘的艺术——什么时候该忘记》</li>
      <li>记忆系统第 6 篇《记忆系统的可观测性与调试》</li>
      <li>如果时间允许：开始情景记忆在 Hermes 中的落地实践</li>
    </ul>

    <hr>

    <p style="font-size: 13px; color: #475569;">学习周记是一个实验——每两周一篇，记录学习轨迹。这不是写给读者的，是给自己的进度条。</p>
  </div>
</Layout>
