---
import Layout from '../../layouts/Layout.astro';
const title = 'ReAct 范式 Agent 的底层循环';
const description = '所有 Agent 框架本质上都在做同一件事：让 LLM 像人一样想一步做一步看一步。';
const date = '2026.07.04';
const tags = ['基础理论', 'Agent范式'];
---

<Layout title={title} description={description}>
  <style>
    .article { max-width: 720px; margin: 0 auto; padding: 60px 24px 80px; }
    .meta { color: #334155; font-size: 13px; margin-bottom: 8px; }
    .tags { display: flex; gap: 6px; margin-bottom: 16px; flex-wrap: wrap; }
    .tag { font-size: 11px; padding: 3px 10px; border-radius: 4px; background: #eef2ff; color: #4338ca; font-weight: 500; }
    h1 { font-size: 32px; font-weight: 700; line-height: 1.3; margin-bottom: 16px; color: #0f172a; }
    .sub { font-size: 15px; color: #334155; line-height: 1.7; border-left: 3px solid #2563eb; padding-left: 16px; margin-bottom: 40px; }
    h2 { font-size: 22px; font-weight: 700; margin: 40px 0 16px; color: #0f172a; padding-bottom: 8px; border-bottom: 1px solid #e2e8f0; }
    h3 { font-size: 17px; font-weight: 600; margin: 28px 0 10px; color: #0f172a; }
    p { font-size: 15px; color: #334155; line-height: 1.8; margin-bottom: 16px; }
    b { color: #0f172a; }
    pre { background: #f8fafc; border: 1px solid #e2e8f0; border-radius: 8px; padding: 16px 20px; font-size: 13px; color: #334155; margin-bottom: 16px; overflow-x: auto; }
    hr { border: none; border-top: 1px solid #e2e8f0; margin: 40px 0; }
    ul { color: #334155; font-size: 15px; line-height: 1.8; margin-bottom: 16px; padding-left: 24px; }
    li { margin-bottom: 4px; }
    .box { background: #ffffff; border: 1.5px solid #e2e8f0; border-radius: 10px; padding: 24px; margin: 24px 0; }
    .box svg { width: 100%; max-width: 520px; display: block; margin: 0 auto; }
    @media (max-width: 768px) { .article { padding: 40px 20px 60px; } h1 { font-size: 26px; } }
  </style>
  <article class="article">
    <div class="tags"><span class="tag">基础理论</span><span class="tag">Agent范式</span></div>
    <h1>{title}</h1>
    <div class="meta">{date}</div>
    <div class="sub">{description}</div>
    <hr>

    <h2>一、ReAct 不是什么</h2>
    <p>在展开之前先澄清一个常见的误解。ReAct 不是 OpenAI 发明的，不是 LangChain 的一个功能，也不是某个特定框架的特性。它是一篇学术论文的核心观点：<b>Reasoning + Acting（推理 + 行动）</b>，由 Shunyu Yao 等人在 2022 年提出。</p>
    <p>论文的核心发现其实很简单：<b>让语言模型在回答问题时先想一想需要做什么，然后做（调用工具或查资料），看到结果后再继续想</b>——这样反复循环效果远远好于让模型直接回答。这个发现后来成了所有 Agent 系统的理论根基。</p>

    <h2>二、ReAct 的核心循环</h2>
    <p>一个 ReAct 循环由三个基本步骤组成：</p>
    <pre><code>Thought（思考）→ Action（行动）→ Observation（观察）→ 再思考 → 循环 → 最终回答</code></pre>

    <div class="box">
      <svg viewBox="0 0 520 240">
        <rect x="190" y="10" width="140" height="44" rx="22" fill="#eef2ff" stroke="#bfdbfe" stroke-width="2"/>
        <text x="260" y="37" text-anchor="middle" fill="#1e40af" font-size="14" font-weight="600">Thought 思考</text>
        <rect x="330" y="95" width="140" height="44" rx="8" fill="#f0fdfa" stroke="#a7f3d0" stroke-width="2"/>
        <text x="400" y="122" text-anchor="middle" fill="#166534" font-size="14" font-weight="600">Action 行动</text>
        <rect x="50" y="95" width="140" height="44" rx="8" fill="#f5f3ff" stroke="#ddd6fe" stroke-width="2"/>
        <text x="120" y="122" text-anchor="middle" fill="#5b21b6" font-size="14" font-weight="600">Observation 观察</text>
        <line x1="260" y1="54" x2="260" y2="80" stroke="#64748b" stroke-width="2"/>
        <polygon points="254,78 266,78 260,86" fill="#64748b"/>
        <line x1="330" y1="105" x2="280" y2="105" stroke="#64748b" stroke-width="2"/>
        <polygon points="282,99 282,111 274,105" fill="#64748b"/>
        <line x1="470" y1="117" x2="500" y2="117" stroke="#64748b" stroke-width="2"/>
        <line x1="500" y1="117" x2="500" y2="32" stroke="#64748b" stroke-width="2"/>
        <line x1="500" y1="32" x2="335" y2="32" stroke="#64748b" stroke-width="2"/>
        <polygon points="337,26 337,38 329,32" fill="#64748b"/>
        <text x="510" y="78" fill="#94a3b8" font-size="10">循环</text>
        <text x="260" y="170" text-anchor="middle" fill="#94a3b8" font-size="10">最终输出（条件满足时）</text>
        <line x1="260" y1="160" x2="260" y2="180" stroke="#cbd5e1" stroke-width="1.5" stroke-dasharray="4,3"/>
        <rect x="200" y="183" width="120" height="24" rx="4" fill="#f1f5f9" stroke="#e2e8f0" stroke-width="1"/>
        <text x="260" y="199" text-anchor="middle" fill="#64748b" font-size="11">最终回答</text>
      </svg>
    </div>

    <h3>2.1 Thought（思考）</h3>
    <p>模型在当前状态下推理下一步应该做什么。这一步输出自然语言——模型自言自语地分析当前情况、判断接下来该调用什么工具、或者已经收集到足够信息可以给出最终答案。例如一个客服 Agent 在处理退换货请求时它的 Thought 可能是：用户提供了订单号需要先查这笔订单的状态。我手上有 lookup_order 工具可以查订单信息。这个思考过程在最终回复给用户时通常会被隐藏但在开发调试阶段可以看到——它是理解 Agent 决策逻辑的关键。</p>

    <h3>2.2 Action（行动）</h3>
    <p>根据思考的结果模型选择一个工具并执行。工具可以是查数据库、调 API、发送消息、计算等任何可编程的操作。Action 的输出格式通常是结构化的：</p>
    <pre><code>工具：lookup_order
参数：&lbrace;"order_id": "ORD-2024-12345"&rbrace;</code></pre>

    <h3>2.3 Observation（观察）</h3>
    <p>工具执行后返回的结果。模型阅读这个结果判断下一步怎么做：如果结果提供了足够的信息就给出最终答案；如果还需要更多信息就继续下一轮 Thought。Observation 通常是工具返回的原始数据模型需要从中提取关键信息。</p>

    <h3>2.4 循环终止条件</h3>
    <p>整个循环持续进行直到以下条件之一满足：模型给出最终答案（并明确表示任务完成）、达到预设的最大步数（防止无限循环）、触发安全边界（模型试图调用不允许的工具）。</p>

    <h2>三、一个完整的 ReAct 例子</h2>
    <p>假设你让一个 Agent 查今天北京的天气适合出门吗背后发生的完整过程：</p>
    <pre><code>用户输入：今天北京的天气适合出门吗？

Round 1:
  Thought: 用户想知道北京的天气。需要先查今天的天气数据。
  Action: get_weather(city="北京")
  Observation: &lbrace;"temp": 32, "humidity": 70, "condition": "晴"&rbrace;

Round 2:
  Thought: 北京今天32度湿度70%。温度较高建议注意防暑。
  Action: 无（直接回答）
  最终输出：北京今天32度天气晴朗温度偏高湿度较大出门建议防暑防晒。</code></pre>
    <p>这个例子展示了 ReAct 的核心价值：<b>模型不需要知道所有事情只需要知道怎么找到答案。</b>它只需要知道有 get_weather 这个工具，通过工具实时获取数据，而不需要预先训练天气数据。</p>

    <h2>四、三种规划模式</h2>
    <p>ReAct 是最基础的模式。在实际工程中根据任务复杂度还有两种变体。</p>

    <h3>4.1 ReAct（边想边做）</h3>
    <pre><code>用户输入 → 思考 → 行动 → 观察 → 思考 → 行动 → ... → 回答</code></pre>
    <p><b>特点：</b>不预先计划每一步都基于当前观察做出决策。<b>适合：</b>简单的步骤不超过三四步的任务。比如查天气、查订单状态。<b>优点：</b>灵活能应对意外。<b>缺点：</b>没有全局视角可能在局部最优上绕圈。</p>

    <h3>4.2 Plan-then-Execute（先计划再执行）</h3>
    <pre><code>用户输入 → 制定完整计划 → 按计划执行 → 回答</code></pre>
    <p><b>特点：</b>模型先输出一份完整的执行计划然后按计划一步步执行不中途调整。<b>适合：</b>步骤明确计划可以一次性确定的流程。<b>优点：</b>效率高 Token 消耗少。<b>缺点：</b>计划有误无法在执行中纠正。</p>

    <h3>4.3 RePlan（动态调整）</h3>
    <pre><code>用户输入 → 制定初步计划 → 执行 → 发现偏差 → 重新计划 → 继续执行</code></pre>
    <p><b>特点：</b>初始计划和执行交替进行——执行一段后根据观察结果重新评估和调整计划。<b>适合：</b>复杂长流程任务。比如多步骤故障排查。<b>优点：</b>兼顾全局视角和灵活性。<b>缺点：</b>实现复杂 Token 消耗最大。</p>

    <h2>五、为什么 ReAct 在企业落地中会遇到挑战</h2>

    <h3>5.1 上下文窗口是硬边界</h3>
    <p>每多一轮循环就消耗更多 Token。一个复杂任务可能需要五到十轮循环这意味着最开始输入的内容会被越来越远的上下文稀释。Agent 在最开始的几轮表现很好但随着循环增多开始忘掉最初的目标或已经获取的信息。</p>
    <p><b>工程对策：</b>定期做上下文摘要把早期内容压缩后重新注入。</p>

    <h3>5.2 工具调用错误的传播</h3>
    <p>ReAct 的一个核心假设是工具的返回结果是正确的。但在真实世界中工具本身可能挂了（API 超时返回空值）、工具返回了模型无法理解的数据格式、模型理解正确但后续步骤基于错误判断做了决策。更麻烦的是模型通常不会主动承认它不确定。它会假装理解然后基于错误信息继续推理。</p>
    <p><b>工程对策：</b>工具返回结果中增加置信度标记；关键步骤做二次验证。</p>

    <h3>5.3 成本随步数线性增长</h3>
    <p>每次 LLM 调用都要钱。一个五步的 ReAct 循环成本是一个一步直答的五到十倍（因为每次调用都要把之前的上下文重新传给模型）。不加限制的 Agent 可能一次任务消耗大量 Token。</p>
    <p><b>工程对策：</b>设置 Token 预算上限、上下文压缩、缓存策略。</p>

    <h2>六、ReAct 与所有 Agent 框架的关系</h2>
    <pre><code>LangGraph 把 ReAct 循环定义为图结构支持分支和条件跳转
CrewAI   多个 ReAct 循环并行通过 Supervisor 协调
Dify     可视化编排 ReAct 循环
AutoGen  多个 Agent 各自跑自己的 ReAct 循环通过对话协调
MCP      标准化 Action 中的工具调用格式</code></pre>
    <p><b>框架是外壳，ReAct 是内核。</b>先理解内核再学外壳事半功倍。</p>

    <hr>
    <p style="font-size:13px; color:#94a3b8;">参考：ReAct 论文（Yao et al. 2022 ReAct: Synergizing Reasoning and Acting in Language Models）</p>
  </article>
</Layout>
