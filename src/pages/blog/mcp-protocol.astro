---
import Layout from '../../layouts/Layout.astro';
const title = 'MCP 协议——工具接入的标准化';
const description = 'Agent 的智能来自模型，但能力来自工具。MCP 要解决的问题是：如何让这些工具被标准化地发现和调用。';
const date = '2026.07.04';
const tags = ['架构设计', 'MCP'];
---

<Layout title={title} description={description}>
  <style>
    .article { max-width: 720px; margin: 0 auto; padding: 60px 24px 80px; }
    .meta { color: #334155; font-size: 13px; margin-bottom: 8px; }
    .tags { display: flex; gap: 6px; margin-bottom: 16px; flex-wrap: wrap; }
    .tag { font-size: 11px; padding: 3px 10px; border-radius: 4px; background: #eef2ff; color: #4338ca; font-weight: 500; }
    h1 { font-size: 32px; font-weight: 700; line-height: 1.3; margin-bottom: 16px; color: #0f172a; }
    .sub { font-size: 15px; color: #334155; line-height: 1.7; border-left: 3px solid #2563eb; padding-left: 16px; margin-bottom: 40px; }
    h2 { font-size: 22px; font-weight: 700; margin: 40px 0 16px; color: #0f172a; padding-bottom: 8px; border-bottom: 1px solid #e2e8f0; }
    h3 { font-size: 17px; font-weight: 600; margin: 28px 0 10px; color: #0f172a; }
    p { font-size: 15px; color: #334155; line-height: 1.8; margin-bottom: 16px; }
    b { color: #0f172a; }
    pre { background: #f8fafc; border: 1px solid #e2e8f0; border-radius: 8px; padding: 16px 20px; font-size: 13px; color: #334155; margin-bottom: 16px; overflow-x: auto; }
    hr { border: none; border-top: 1px solid #e2e8f0; margin: 40px 0; }
    ul { color: #334155; font-size: 15px; line-height: 1.8; margin-bottom: 16px; padding-left: 24px; }
    li { margin-bottom: 4px; }
    .box { background: #ffffff; border: 1.5px solid #e2e8f0; border-radius: 10px; padding: 24px; margin: 24px 0; }
    .box svg { width: 100%; max-width: 500px; display: block; margin: 0 auto; }
    blockquote { border-left: 3px solid #2563eb; padding: 12px 16px; margin: 24px 0; background: #f8fafc; border-radius: 0 8px 8px 0; color: #334155; font-size: 14px; }
    @media (max-width: 768px) { .article { padding: 40px 20px 60px; } h1 { font-size: 26px; } }
  </style>
  <article class="article">
    <div class="tags"><span class="tag">架构设计</span><span class="tag">MCP</span></div>
    <h1>{title}</h1>
    <div class="meta">{date}</div>
    <div class="sub">{description}</div>
    <hr>

    <h2>一、为什么需要 MCP</h2>
    <p>在 MCP 出现之前，Agent 的工具接入是这样的：每个框架都有自己的工具定义方式。换一个 Agent 平台，所有工具要重新实现。这种碎片化带来的问题很具体：重复开发、不可移植、生态隔离。</p>
    <p>这不是某个框架的问题——而是 Agent 生态早期的正常状态。在 RESTful API 普及之前，Web 服务的集成也是类似的情况。</p>

    <h2>二、MCP 是什么</h2>
    <p>MCP（Model Context Protocol）是 2025 年 11 月由 Anthropic 提出的开放协议。它的定位很明确：<b>Agent 世界的标准化工具接入协议。</b></p>
    <blockquote>USB-C 让各种外设用同一个接口连接电脑。MCP 让各种工具用同一个协议接入 Agent。</blockquote>
    <p><b>核心思想：</b>工具提供方只需要实现一个 MCP Server，任何支持 MCP 的 Agent 都能自动发现和调用这些工具。到 2026 年上半年，OpenAI、Google、Microsoft 等主要厂商陆续表达了支持或兼容意向。</p>

    <h2>三、MCP 的核心架构</h2>
    <p>MCP 采用三层架构：</p>

    <div class="box">
      <svg viewBox="0 0 500 220">
        <rect x="150" y="10" width="200" height="40" rx="8" fill="#eef2ff" stroke="#bfdbfe" stroke-width="2"/>
        <text x="250" y="35" text-anchor="middle" fill="#1e40af" font-size="14" font-weight="600">Host（Agent 运行环境）</text>
        <rect x="170" y="58" width="160" height="28" rx="4" fill="#ffffff" stroke="#cbd5e1" stroke-width="1"/>
        <text x="250" y="76" text-anchor="middle" fill="#64748b" font-size="11">MCP Client</text>
        <line x1="250" y1="50" x2="250" y2="56" stroke="#64748b" stroke-width="1.5"/>
        <line x1="250" y1="86" x2="250" y2="105" stroke="#64748b" stroke-width="1.5" stroke-dasharray="4,3"/>
        <rect x="150" y="108" width="200" height="36" rx="8" fill="#f0fdfa" stroke="#a7f3d0" stroke-width="2"/>
        <text x="250" y="131" text-anchor="middle" fill="#166534" font-size="13" font-weight="600">MCP Server</text>
        <line x1="250" y1="144" x2="250" y2="152" stroke="#64748b" stroke-width="1.5"/>
        <rect x="80" y="155" width="130" height="32" rx="6" fill="#f5f3ff" stroke="#ddd6fe" stroke-width="1.5"/>
        <text x="145" y="175" text-anchor="middle" fill="#5b21b6" font-size="12" font-weight="600">工具 A（查数据库）</text>
        <rect x="290" y="155" width="130" height="32" rx="6" fill="#f5f3ff" stroke="#ddd6fe" stroke-width="1.5"/>
        <text x="355" y="175" text-anchor="middle" fill="#5b21b6" font-size="12" font-weight="600">工具 B（发消息）</text>
        <rect x="120" y="192" width="260" height="24" rx="4" fill="#f1f5f9" stroke="#e2e8f0" stroke-width="1"/>
        <text x="250" y="208" text-anchor="middle" fill="#94a3b8" font-size="10">JSON-RPC 通信</text>
      </svg>
    </div>

    <h3>Host</h3>
    <p>Host 是 Agent 的运行环境——LLM 应用程序本身。它负责加载 MCP Client、连接 MCP Server、为 LLM 提供工具列表。</p>

    <h3>Client</h3>
    <p>Client 是 Host 和 Server 之间的通信层。它负责：发现 Server 的工具列表、将工具信息转为 LLM 可理解的格式、转发 tool call 请求、返回执行结果。</p>

    <h3>Server</h3>
    <p>Server 是工具的实际提供方。每个 Server 是一个独立进程，注册一个或多个工具。Server 声明自己有哪些工具、接收调用请求并执行、返回结果。<b>关键设计：</b>Server 独立进程运行，与 Host 隔离，即使崩溃也不影响主进程。</p>

    <h3>通信方式</h3>
    <p>MCP 使用 JSON-RPC 协议。一个典型调用流程：Host 启动时连接 Server → 获取工具列表 → LLM 决定调用工具 → Host 发送 JSON-RPC 请求到 Server → Server 执行并返回结果 → Host 将结果给 LLM。</p>

    <h2>四、MCP 解决了什么问题</h2>

    <h3>4.1 工具发现</h3>
    <p>在 MCP 之前，Agent 需要用哪些工具是在代码里写死的。MCP 引入运行时发现机制：Host 连接 Server 后，Server 主动声明所有支持的工具。新增工具只需要启动新的 MCP Server，不需要修改 Agent 代码。</p>

    <h3>4.2 标准化调用</h3>
    <p>所有工具的调用方式统一：JSON Schema 描述参数、JSON 格式返回值、标准错误码、stdio 或 HTTP+SSE 传输。开发者只关心工具逻辑，不关心 LLM 怎么调用。</p>

    <h3>4.3 安全边界</h3>
    <p>每个 MCP Server 是独立进程，与 Agent 主进程隔离。Server 崩溃不影响 Agent，Server 的权限可以单独控制，Agent 无法直接访问 Server 内部状态。类似于微服务架构的隔离原则。</p>

    <h2>五、MCP 与 A2A 的关系</h2>
    <p>2026 年 6 月 Google 发布了 A2A（Agent-to-Agent）协议。两者互补：</p>
    <pre><code>MCP → 工具接入标准化（Agent 怎么调用工具）
A2A → Agent 通信标准化（Agent 之间怎么对话）</code></pre>
    <p>两者结合构成了 Agent 生态的完整通信层。</p>

    <h2>六、展望</h2>
    <p>MCP 从提出到获得主要厂商支持不到一年。如果这个趋势持续，2027 年的 Agent 开发体验将类似于今天的 RESTful API 开发：工具一次开发到处运行、工具市场出现、主流框架内建 MCP Client 支持。</p>
    <p>当然标准化从来不是一蹴而就的。MCP 还需要解决协议版本管理、Server 认证与授权、跨网络传输等问题。但方向已经明确——<b>碎片化的工具接入正在走向标准化，这对整个 Agent 生态都是好事。</b></p>

    <hr>
    <p style="font-size:13px; color:#94a3b8;">参考：Anthropic MCP 协议发布（2025.11）、Google A2A 协议发布（2026.06）</p>
  </article>
</Layout>
