---
import Layout from '../../layouts/Layout.astro';
const title = '三种编排模式与选型决策';
const description = '搭 Agent 系统时，第一个要回答的问题：一个 Agent 还是多个 Agent？谁来管？怎么管？';
const date = '2026.07.04';
const tags = ['架构设计', '编排模式'];
---

<Layout title={title} description={description}>
  <style>
    .article { max-width: 720px; margin: 0 auto; padding: 60px 24px 80px; }
    .meta { color: #334155; font-size: 13px; margin-bottom: 8px; }
    .tags { display: flex; gap: 6px; margin-bottom: 16px; flex-wrap: wrap; }
    .tag { font-size: 11px; padding: 3px 10px; border-radius: 4px; background: #eef2ff; color: #4338ca; font-weight: 500; }
    h1 { font-size: 32px; font-weight: 700; line-height: 1.3; margin-bottom: 16px; color: #0f172a; }
    .sub { font-size: 15px; color: #334155; line-height: 1.7; border-left: 3px solid #2563eb; padding-left: 16px; margin-bottom: 40px; }
    h2 { font-size: 22px; font-weight: 700; margin: 40px 0 16px; color: #0f172a; padding-bottom: 8px; border-bottom: 1px solid #e2e8f0; }
    h3 { font-size: 17px; font-weight: 600; margin: 28px 0 10px; color: #0f172a; }
    p { font-size: 15px; color: #334155; line-height: 1.8; margin-bottom: 16px; }
    b { color: #0f172a; }
    pre { background: #f8fafc; border: 1px solid #e2e8f0; border-radius: 8px; padding: 16px 20px; font-size: 13px; color: #334155; margin-bottom: 16px; overflow-x: auto; }
    hr { border: none; border-top: 1px solid #e2e8f0; margin: 40px 0; }
    ul { color: #334155; font-size: 15px; line-height: 1.8; margin-bottom: 16px; padding-left: 24px; }
    li { margin-bottom: 4px; }
    .box { background: #ffffff; border: 1.5px solid #e2e8f0; border-radius: 10px; padding: 24px; margin: 24px 0; }
    .box svg { width: 100%; max-width: 100%; display: block; margin: 0 auto; }
    @media (max-width: 768px) { .article { padding: 40px 20px 60px; } h1 { font-size: 26px; } }
  </style>
  <article class="article">
    <div class="tags"><span class="tag">架构设计</span><span class="tag">编排模式</span></div>
    <h1>{title}</h1>
    <div class="meta">{date}</div>
    <div class="sub">{description}</div>
    <hr>

    <h2>一、为什么编排模式是第一个要做的决策</h2>
    <p>假设你要搭一个 Agent 系统来处理客户订单。任务涉及查库存、验证支付、安排发货、发送通知。你可以把所有能力塞进一个 Agent 给十几个工具。也可以拆成几个专门的 Agent——一个管库存、一个管支付、一个管物流再加一个管协调的。两种做法都有人用但选错了后面改起来成本很高。</p>
    <p><b>编排模式就是回答这个问题的框架：Agent 之间怎么组织、谁听谁的、信息怎么流动。</b></p>

    <h2>二、模式一：中心化编排（Orchestrator）</h2>
    <p><b>结构：</b>一个中央 Agent（Orchestrator）负责任务分解和结果汇总，多个 Worker Agent 各司其职。Worker 之间不直接通信。</p>

    <div class="box">
    <svg viewBox="0 0 500 160">
    <rect x="175" y="10" width="150" height="36" rx="8" fill="#eef2ff" stroke="#bfdbfe" stroke-width="2"/>
    <text x="250" y="33" text-anchor="middle" fill="#1e40af" font-size="13" font-weight="600">Orchestrator 主控</text>
    <line x1="250" y1="46" x2="250" y2="56" stroke="#64748b" stroke-width="1.5"/>
    <line x1="250" y1="56" x2="120" y2="70" stroke="#64748b" stroke-width="1.5"/>
    <line x1="250" y1="56" x2="250" y2="70" stroke="#64748b" stroke-width="1.5"/>
    <line x1="250" y1="56" x2="380" y2="70" stroke="#64748b" stroke-width="1.5"/>
    <rect x="50" y="72" width="140" height="34" rx="6" fill="#f0fdfa" stroke="#a7f3d0" stroke-width="1.5"/>
    <text x="120" y="93" text-anchor="middle" fill="#166534" font-size="12">Worker A（库存查询）</text>
    <rect x="180" y="72" width="140" height="34" rx="6" fill="#f0fdfa" stroke="#a7f3d0" stroke-width="1.5"/>
    <text x="250" y="93" text-anchor="middle" fill="#166534" font-size="12">Worker B（支付验证）</text>
    <rect x="310" y="72" width="140" height="34" rx="6" fill="#f0fdfa" stroke="#a7f3d0" stroke-width="1.5"/>
    <text x="380" y="93" text-anchor="middle" fill="#166534" font-size="12">Worker C（物流安排）</text>
    <line x1="120" y1="106" x2="250" y2="116" stroke="#94a3b8" stroke-width="1" stroke-dasharray="4,3"/>
    <line x1="250" y1="106" x2="250" y2="116" stroke="#94a3b8" stroke-width="1" stroke-dasharray="4,3"/>
    <line x1="380" y1="106" x2="250" y2="116" stroke="#94a3b8" stroke-width="1" stroke-dasharray="4,3"/>
    <rect x="190" y="118" width="120" height="28" rx="6" fill="#f1f5f9" stroke="#e2e8f0" stroke-width="1"/>
    <text x="250" y="136" text-anchor="middle" fill="#64748b" font-size="11">汇总结果 → 用户</text>
    </svg>
    </div>

    <p><b>代表框架：</b>CrewAI、AutoGen</p>

    <h3>真实案例：Hermes Agent</h3>
    <p>Hermes Agent（Nous Research 开发）采用 Supervisor-Worker 模式。主 Agent 通过 delegate_task 将任务分解给子 Agent，子 Agent 独立执行后返回结果。主 Agent 不做具体执行只做任务分解和结果汇总。每个子 Agent 拥有独立的工具集和上下文互不干扰。这是中心化编排在生产中的典型应用。</p>
    <pre><code>主 Hermes Agent → delegate_task(goal="查系统状态")
  ├── 子 Agent A（SSH 查服务器）
  ├── 子 Agent B（查数据库）
  └── 子 Agent C（生成报告）
  ← 汇总结果 → 用户</code></pre>

    <h3>真实案例：OpenClaw</h3>
    <p>OpenClaw 是开源通用 AI 助手（GitHub: openclaw/openclaw），支持 WhatsApp、Telegram、Discord、Slack 等多种平台。它的架构在宏观层面采用中心化编排（主控 Agent 负责意图识别和任务分发），在具体执行层面灵活切换——简单查询走单 Agent 直答，复杂任务动态调度子 Agent。这种混合模式使其能处理从发一条消息到跨平台协调多个任务的广泛场景。</p>

    <p><b>优点：</b>职责清晰、可审计、Worker 可复用。<b>缺点：</b>Orchestrator 上下文窗口容易超载。</p>

    <h2>三、模式二：去中心化协作（Swarm）</h2>
    <p><b>结构：</b>没有中心控制节点。Agent 之间直接对话协商传递任务。每个 Agent 知道自己能做什么也知道谁能做什么。</p>
    <pre><code>Agent A ←→ Agent B
  ↕         ↕
Agent C ←→ Agent D</code></pre>
    <p><b>代表框架：</b>ChatDev、Agent Protocol、Google A2A<br/>
    <b>优点：</b>扩展性好、无单点故障。<b>缺点：</b>调试困难、无法保证全局一致性。</p>

    <h2>四、模式三：DAG 流水线（Pipeline）</h2>
    <p><b>结构：</b>预定义执行图。每个节点是独立处理步骤，输入输出有明确 Schema。数据按预定义路径流动。</p>
    <pre><code>步骤1（查库存）→ 步骤2（验证支付）→ 步骤3（安排发货）→ 步骤4（发送通知）
         ↓（库存不足）
        步骤2b（通知客户缺货）</code></pre>
    <p><b>代表框架：</b>LangGraph、Temporal<br/>
    <b>优点：</b>可观测性最强、可人工介入、容错好。<b>缺点：</b>灵活性不如前两者、不适合动态决策。</p>

    <h2>五、选型决策树</h2>

    <div class="box">
    <svg viewBox="0 0 520 200">
    <text x="260" y="20" text-anchor="middle" fill="#475569" font-size="12" font-weight="600">任务步骤是否固定？</text>
    <polygon points="252,24 268,24 260,32" fill="#475569"/>
    <line x1="260" y1="35" x2="170" y2="60" stroke="#94a3b8" stroke-width="1.5"/>
    <line x1="260" y1="35" x2="350" y2="60" stroke="#94a3b8" stroke-width="1.5"/>
    <text x="215" y="52" fill="#16a34a" font-size="11" font-weight="600">是</text>
    <text x="310" y="52" fill="#dc2626" font-size="11" font-weight="600">否</text>
    <rect x="95" y="62" width="150" height="32" rx="8" fill="#f0fdfa" stroke="#a7f3d0" stroke-width="1.5"/>
    <text x="170" y="82" text-anchor="middle" fill="#166534" font-size="12" font-weight="600">Pipeline（DAG 流水线）</text>
    <text x="260" y="110" text-anchor="middle" fill="#475569" font-size="12" font-weight="600">是否需要全局协调？</text>
    <polygon points="252,114 268,114 260,122" fill="#475569"/>
    <line x1="260" y1="125" x2="170" y2="148" stroke="#94a3b8" stroke-width="1.5"/>
    <line x1="260" y1="125" x2="350" y2="148" stroke="#94a3b8" stroke-width="1.5"/>
    <text x="215" y="143" fill="#16a34a" font-size="11" font-weight="600">是</text>
    <text x="310" y="143" fill="#dc2626" font-size="11" font-weight="600">否</text>
    <rect x="95" y="150" width="150" height="32" rx="8" fill="#eef2ff" stroke="#bfdbfe" stroke-width="1.5"/>
    <text x="170" y="170" text-anchor="middle" fill="#1e40af" font-size="12" font-weight="600">Orchestrator</text>
    <rect x="275" y="150" width="150" height="32" rx="8" fill="#f5f3ff" stroke="#ddd6fe" stroke-width="1.5"/>
    <text x="350" y="170" text-anchor="middle" fill="#5b21b6" font-size="12" font-weight="600">Swarm</text>
    </svg>
    </div>
    <p>但有经验的团队通常不会只用一种模式。最实战的组合：<b>Orchestrator + Pipeline 混合。</b></p>
    <pre><code>Router（意图识别）
  ├── 步骤明确的流程 → Pipeline（LangGraph 执行）
  ├── 需要多知识领域 → Orchestrator + Worker
  └── 简单查询 → 单 Agent 直接回答</code></pre>
    <p>先用轻量 Router 做意图分类，根据任务类型路由到不同执行模式。简单查询走单 Agent，固定流程走 Pipeline，复杂任务走 Orchestrator+Worker。</p>

    <h2>六、小结</h2>
    <pre><code>中心化编排：一个大脑管全局 → 适合复杂多步骤 → CrewAI / Hermes
去中心化协作：Agent 直接对话 → 适合专家协商 → ChatDev / A2A
DAG 流水线：预定义执行图 → 适合固定流程 → LangGraph / Temporal
实战组合：Router + Pipeline + Orchestrator 混合 → OpenClaw</code></pre>
    <p>没有最好的模式，只有最合适的模式。关键是根据任务特征选择而不是根据个人偏好。</p>

    <hr>
  </article>
</Layout>
